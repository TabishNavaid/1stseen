/**
 * The policy pages: terms, privacy, Google data use, and data sources. They are drafts for the owner's review and
 * are not legal advice. Set this to false once they have been reviewed: every one of those pages shows or drops its
 * "Draft for review" marker from this one constant. The contact page carries no marker; it states only what the
 * deployment is configured with.
 */
export const LEGAL_PAGES_ARE_DRAFTS = true;

/** When the policy pages last changed in substance, shown on each so a reader can tell whether they moved. */
export const LEGAL_PAGES_UPDATED = "2026-09-17";
