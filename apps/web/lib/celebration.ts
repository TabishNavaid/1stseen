/**
 * Whether the reader asked for less movement, decided without React so the tests can pin it.
 *
 * The first run used to answer a save with a burst of twelve pieces. It does not any more: a burst is a lot of noise
 * about something the product has not finished doing for anyone yet, and one bird and one stamp say it better.
 */

export const REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)";
